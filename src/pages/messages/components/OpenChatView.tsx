import { cn } from "/src/lib/utils";

export default function OpenChatView({ className }: { className?: string }) {
  return (
    <div className={cn("max-lg:absolute", className)}>

    </div>
  )
}
